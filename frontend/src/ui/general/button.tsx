import styled from 'styled-components'
import {
  typography,
  layout,
  space,
  color,
  border,
} from 'styled-system'

export const Button = styled.button`
  ${typography}
  ${layout}
  ${space}
  ${color}
  ${border}
`

