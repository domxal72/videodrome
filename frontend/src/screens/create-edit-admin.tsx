import React from 'react'
import { Flex } from '../ui/general/flex'

export default function CreateEditAdmin() {
  return (
    <Flex>
      {/* TODO */}
      {/* Must be logged in as Admin to access this screen and edit it */}
    </Flex>
  )
}
